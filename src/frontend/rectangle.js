class Kare {
  
  constructor(startx, starty, width,height) {
    this.startx = startx;
    this.starty = starty;
    this.width = width;
    this.height = height;
    
  }

  
  
}

export default Kare;